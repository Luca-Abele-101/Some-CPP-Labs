#ifndef TASKS_HPP
#define TASKS_HPP

#include <string>
#include <vector>
#include <iostream>

namespace lutsaj
{
  void task1(const std::string& order);
  void task2(const char* filename);
  void task3();
  void task4(const std::string& order, int n);
}
#endif
